# Orator AI

Orator AI — это полнофункциональное production-ready AI-приложение для голосовых челленджей, обратной связи и оплаты по использованию.

## 📦 Структура проекта

```
/app                # Next.js App Router, страницы (регистрация, вход, дашборд, вызовы, история, результаты)
/components         # UI-компоненты (шапка, формы, кнопки, кредиты и т.д.)
/pages/api          # API-роуты (аутентификация, платежи, вызовы, история, кредиты)
/drizzle            # ORM-схемы и подключение к SQLite
/lib                # Вспомогательные библиотеки (auth, payments, utils)
/storage/sessions   # Локальное хранилище WAV-файлов
/public             # Публичные ассеты
```

## 🧩 Основные возможности
- Next.js 14 (App Router, React Server Components, Server Actions)
- Tailwind CSS + shadcn/ui
- next-auth (JWT, Drizzle ORM)
- Stripe, Paddle, Generic API (оплата, кредиты)
- SQLite (через Drizzle ORM)
- ElevenLabs TTS/LLM (интеграция для звонков)
- WebRTC (основа для живых звонков)
- Локальное хранение WAV-файлов
- Docker, Railway-ready

## 🗂️ Основные страницы
- `/register`, `/login` — регистрация и вход
- `/dashboard` — список челленджей, покупка кредитов
- `/challenge/[id]` — детали челленджа, старт звонка
- `/call` — экран звонка (визуализация аудио, завершение)
- `/result` — обратная связь по сессии
- `/history` — история звонков пользователя

## ⚙️ Развёртывание и запуск

### 1. Клонируйте репозиторий
```bash
git clone <ваш-репозиторий>
cd ORATOR
```

### 2. Настройте переменные окружения
- Скопируйте `.env.example` в `.env` и заполните все ключи (Stripe, Paddle, ElevenLabs, NextAuth и др.)

### 3. Установите зависимости
```bash
npm install
# или
yarn install
```

### 4. Локальный запуск
```bash
npm run dev
```

### 5. Docker (production)
```bash
docker-compose up --build
```

### 6. Railway
- Подключите репозиторий к Railway
- Установите переменные окружения из `.env.example`
- Запустите деплой

## 📝 Примечания
- WAV-файлы хранятся локально в `/storage/sessions/`
- SQLite база данных — `/storage/orator.sqlite`
- Для production рекомендуется использовать Railway или любой Docker-совместимый хостинг

## 🚀 Краткое описание архитектуры
- **Аутентификация:** next-auth (JWT, Drizzle ORM, SQLite)
- **Платежи:** Stripe, Paddle, Generic API (webhook + кредиты)
- **Челленджи и звонки:** создание сессии, списание кредита, WebRTC/ElevenLabs, обратная связь
- **UI:** Современный дизайн на Tailwind + shadcn/ui, адаптивная шапка и навигация
- **API:** Все бизнес-логика через Next.js API Routes
- **ORM:** Drizzle ORM, строгие схемы, миграции не требуются (SQLite)

## 📄 Лицензия
MIT 