# 🎤 Microphone Troubleshooting Guide

Если при использовании `test-scribe-realtime.js --mic` вы получаете "No transcription received", следуйте этому руководству.

---

## ✅ Что работает

Транскрипция с **файлами** работает отлично:

```bash
$ node test-scribe-realtime.js ./storage/sessions/test.pcm
✅ Transcription SUCCESS!
📝 Transcript: "Эй, эй, бой, эй, тест, тест."
```

---

## ❌ Проблема: "No transcription received" в режиме микрофона

### Возможные причины:

## 1. 🎙️ Микрофон не захватывает звук

### Проверка:
```bash
# Запустить скрипт
node test-scribe-realtime.js --mic

# Проверить вывод:
# - Chunks sent: 0 или очень мало → микрофон не работает
# - Chunks sent: много → микрофон работает, но нет речи
```

### Решение:

**macOS:**
1. Откройте **Системные настройки** → **Конфиденциальность и безопасность** → **Микрофон**
2. Разрешите доступ для **Terminal** или вашего терминала
3. Перезапустите терминал

**Linux:**
```bash
# Проверить что микрофон виден
arecord -l

# Протестировать запись
arecord -d 5 test.wav
aplay test.wav
```

**Тест записи через SoX:**
```bash
# Запись 5 секунд с микрофона
rec -r 16000 -c 1 test.wav

# Воспроизведение
play test.wav
```

---

## 2. 🔇 Микрофон работает, но вы не говорите / говорите слишком тихо

### Проверка:
```bash
# При запуске скрипта смотрите на:
Chunks sent: XXX

# Если chunks отправляются (> 100), но транскрипции нет:
# - Говорите громче
# - Проверьте уровни записи в системе
```

### Решение:

**macOS:**
1. **Системные настройки** → **Звук** → **Вход**
2. Проверьте **Громкость входа** (должна быть высокая)
3. Говорите в микрофон и смотрите на индикатор уровня

**Linux (PulseAudio):**
```bash
# Открыть микшер
pavucontrol

# Или через командную строку
pactl set-source-volume @DEFAULT_SOURCE@ 100%
```

---

## 3. 🔧 SoX не настроен правильно

### Проверка:
```bash
sox --version
# Должно показать версию SoX

rec --help
# Должно показать help для записи
```

### Решение:

**Переустановить SoX:**

```bash
# macOS
brew uninstall sox
brew install sox

# Linux
sudo apt-get remove sox
sudo apt-get install sox libsox-fmt-all
```

---

## 4. 📦 Модуль `mic` не работает

### Проверка:
```bash
npm list mic
# Должен показать установленную версию
```

### Решение:

```bash
# Переустановить mic
npm uninstall mic
npm install mic --save

# Протестировать
node test-scribe-realtime.js --mic
```

---

## 5. ⚡ Слишком быстро завершили запись

### Проблема:
Если нажать Q/ESC сразу после начала записи, аудио не успеет обработаться.

### Решение:

1. Запустите скрипт: `node test-scribe-realtime.js --mic`
2. **Дождитесь** сообщения: `🎬 Transcription session started!`
3. **Говорите 3-5 секунд**
4. Подождите 1-2 секунды после речи
5. Нажмите `Q` или `ESC`

---

## 🔍 Режим отладки

Включите подробный вывод:

```bash
DEBUG_WS=1 node test-scribe-realtime.js --mic
```

Вы увидите все WebSocket сообщения:
```
[DEBUG] Message #1: session_started
[DEBUG] Message #2: partial_transcript
[DEBUG] Message #3: committed_transcript
```

---

## ✅ Правильная последовательность действий

### Шаг 1: Запустить скрипт
```bash
node test-scribe-realtime.js --mic
```

### Шаг 2: Дождаться готовности
```
[Mic] 🔌 Connecting to Scribe WebSocket...
[Mic] ✅ WebSocket connected!
[Mic] 🎬 Transcription session started!  ← ЖДИТЕ ЭТОГО!

┌──────────────────────────────────────────────────────┐
│  🔴 RECORDING - Speak into your microphone          │
└──────────────────────────────────────────────────────┘
```

### Шаг 3: Говорить чётко и громко
```
Говорите 3-5 секунд
Например: "Привет, это тест микрофона для транскрипции"
```

### Шаг 4: Увидеть partial транскрипт
```
💬 Привет это тест микрофона...  ← Должно появиться!
```

### Шаг 5: Нажать Enter для commit (опционально)
```
[Mic] 📌 Committing current phrase...
✅ Привет это тест микрофона
```

### Шаг 6: Завершить запись
```
Нажмите Q или ESC

[Mic] ⏹️ Finishing session...
[Mic] 📤 Sending final commit...

============================================================
📝 FULL TRANSCRIPT:

Привет это тест микрофона для транскрипции

📊 Stats:
   - Characters: 43
   - Words: 6
   - Chunks sent: 234
   - Messages received: 4
============================================================
```

---

## 🧪 Тестовый сценарий

### Тест 1: Проверка с файлом (должен работать)
```bash
node test-scribe-realtime.js ./storage/sessions/18c5a86e-8bb9-4fab-a78f-1579807b702c.pcm
```

**Ожидаемый результат:**
```
✅ [Committed] Эй, эй, бой, эй, тест, тест.
📝 FULL TRANSCRIPT: Эй, эй, бой, эй, тест, тест.
```

### Тест 2: Проверка микрофона через SoX
```bash
# Записать 5 секунд
rec -r 16000 -c 1 -b 16 test-mic.wav trim 0 5

# Воспроизвести
play test-mic.wav
```

**Если слышите свой голос** → микрофон работает!

### Тест 3: Запись с микрофона через скрипт
```bash
node test-scribe-realtime.js --mic

# 1. Дождаться "Transcription session started!"
# 2. Сказать громко: "Раз два три четыре пять"
# 3. Подождать 2 секунды
# 4. Нажать Q
```

---

## 📊 Диагностическая информация

При запуске с микрофоном, обратите внимание на:

```
🔍 Debug info:
   - Chunks sent: 234          ← Должно быть > 100 для 3-5 сек речи
   - Messages received: 4      ← Должно быть > 2
   - Last partial text: ...    ← Должен быть текст
```

### Что означают значения:

| Chunks sent | Messages | Partial text | Диагноз |
|-------------|----------|--------------|---------|
| 0 | 1-2 | (none) | Микрофон не работает / нет разрешений |
| < 50 | 1-2 | (none) | Запись слишком короткая |
| > 100 | 1-2 | (none) | Говорили слишком тихо / нет речи |
| > 100 | > 2 | text | ✅ Всё работает! |

---

## 🆘 Если ничего не помогает

### 1. Проверьте API ключ
```bash
cat .env | grep ELEVENLABS_API_KEY
# Должен быть валидный ключ
```

### 2. Проверьте лимиты ElevenLabs
```bash
node test-elevenlabs-api.js limits
# Убедитесь что не превышен лимит символов
```

### 3. Используйте файловый режим
```bash
# Запишите аудио отдельно
rec -r 16000 -c 1 my-test.wav trim 0 5

# Конвертируйте в PCM
ffmpeg -i my-test.wav -ar 16000 -ac 1 -f s16le my-test.pcm

# Транскрибируйте файл
node test-scribe-realtime.js my-test.pcm
```

---

## 📞 Поддержка

Если проблема остаётся:

1. Запустите с отладкой: `DEBUG_WS=1 node test-scribe-realtime.js --mic`
2. Сохраните вывод
3. Проверьте:
   - Версию Node.js: `node --version` (должна быть >= 18)
   - Версию SoX: `sox --version`
   - Версию mic: `npm list mic`

---

## ✅ Быстрая проверка

```bash
# 1. Проверить зависимости
sox --version && node --version && npm list mic

# 2. Проверить API ключ
cat .env | grep ELEVENLABS_API_KEY

# 3. Протестировать с файлом
node test-scribe-realtime.js ./storage/sessions/18c5a86e-8bb9-4fab-a78f-1579807b702c.pcm

# 4. Если файл работает - протестировать микрофон через SoX
rec -r 16000 -c 1 test.wav trim 0 5 && play test.wav

# 5. Если SoX работает - запустить скрипт с микрофоном
node test-scribe-realtime.js --mic
```

---

**Последнее обновление:** 2025-01-17
**Протестировано на:** macOS (M-series), Linux (Ubuntu 22.04)
