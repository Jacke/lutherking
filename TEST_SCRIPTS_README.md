# 🧪 Test Scripts Overview

Коллекция тестовых скриптов для работы с ElevenLabs API и транскрипцией.

## 📁 Файлы

### 1. `test-elevenlabs-api.js` - API Information Tool
**Назначение:** Получение информации об аккаунте ElevenLabs

**Использование:**
```bash
# Информация о пользователе
node test-elevenlabs-api.js user

# Лимиты использования
node test-elevenlabs-api.js limits

# Доступные модели
node test-elevenlabs-api.js models

# Доступные голоса
node test-elevenlabs-api.js voices

# Вся информация сразу
node test-elevenlabs-api.js all
```

**Что показывает:**
- ✅ Тип подписки и статус
- ✅ Использование символов (с прогресс-баром)
- ✅ Лимиты голосов
- ✅ Доступные функции
- ✅ Список моделей (10 моделей)
- ✅ Список голосов (разделение по категориям)

**Пример вывода:**
```
💳 SUBSCRIPTION:
  - Tier: starter
  - Character Count: 64
  - Character Limit: 40,000
  - Usage: 0.16%
  - Progress: [░░░░░░░░░░░░░░░░░░░░]
  - Resets on: 12/16/2025
```

---

### 2. `test-scribe-realtime.js` - Real-time Transcription
**Назначение:** Тестирование ElevenLabs Scribe v2 в реальном времени

**Два режима работы:**

#### A. Режим с файлом (оригинальный)
```bash
node test-scribe-realtime.js [path-to-pcm-file]
```

#### B. Режим с микрофоном (новый!)
```bash
node test-scribe-realtime.js --mic
# или
node test-scribe-realtime.js -m
```

**Требования для режима микрофона:**
```bash
# Установить модуль mic
npm install mic

# Установить SoX (аудио-движок)
# macOS:
brew install sox

# Linux:
sudo apt-get install sox libsox-fmt-all

# Windows:
# Скачать с https://sourceforge.net/projects/sox/
```

**Управление в режиме микрофона:**
- `Enter` - Зафиксировать текущую фразу (commit)
- `Q` или `ESC` - Остановить запись и показать результаты
- `Ctrl+C` - Принудительный выход

**Особенности:**
- ✅ Поддержка русского языка (`language_code=ru`)
- ✅ Потоковая передача аудио (real-time)
- ✅ Частичные транскрипты (обновляются в одной строке)
- ✅ Финальные транскрипты (новая строка для каждого)
- ✅ Полная статистика в конце

**Пример вывода:**
```
┌──────────────────────────────────────────────────────┐
│  🔴 RECORDING - Speak into your microphone          │
│  Controls:                                           │
│    Enter - Commit current phrase                     │
│    Q/ESC - Stop recording & show results             │
│  Ctrl+C  - Force exit                                │
└──────────────────────────────────────────────────────┘

💬 Привет это тест...  (обновляется в реальном времени)
✅ Привет это тест
💬 Как дела?...
✅ Как дела?

============================================================
📝 FULL TRANSCRIPT:

Привет это тест Как дела?

📊 Stats:
   - Characters: 24
   - Words: 4
   - Chunks sent: 156
============================================================
```

---

### 3. `test-scribe-api.js` - File-based Transcription API
**Назначение:** Тестирование Scribe API с файлами

```bash
node test-scribe-api.js [audio-file]
```

**Поддерживаемые форматы:**
- WAV
- MP3
- WebM
- И другие аудио форматы

---

## 🔑 Конфигурация

### ✅ Все скрипты используют переменные окружения

API ключи загружаются из файла `.env`:

```javascript
require('dotenv').config();
const ELEVENLABS_API_KEY = process.env.ELEVENLABS_API_KEY;
```

### Как настроить API ключи:

1. **Скопируйте example файл:**
   ```bash
   cp .env.example .env
   ```

2. **Откройте .env и добавьте ключи:**
   ```env
   ELEVENLABS_API_KEY=sk_your_api_key_here
   OPENAI_API_KEY=sk-proj-your_key_here
   ```

3. **Проверьте что ключи загружаются:**
   ```bash
   node test-elevenlabs-api.js user
   ```

### Получить API ключи:
- ElevenLabs: https://elevenlabs.io/app/settings/api-keys
- OpenAI: https://platform.openai.com/api-keys

**Важно:** Никогда не коммитьте файл `.env` в git!

## 📊 Текущий статус аккаунта

По данным на сегодня:

```
✅ Подписка: Starter (активна)
✅ Лимит символов: 40,000
✅ Использовано: 64 (0.16%)
✅ Осталось: 39,936
✅ Сброс лимита: 16.12.2025

✅ Лимит голосов: 10
✅ Мгновенное клонирование: Да
❌ Профессиональное клонирование: Нет
```

## 🎯 Use Cases

### 1. Проверить лимиты перед использованием
```bash
node test-elevenlabs-api.js limits
```

### 2. Записать и транскрибировать с микрофона
```bash
node test-scribe-realtime.js --mic
```

### 3. Транскрибировать существующий файл
```bash
node test-scribe-realtime.js ./path/to/audio.pcm
```

### 4. Посмотреть доступные модели
```bash
node test-elevenlabs-api.js models
```

### 5. Узнать когда сбросятся лимиты
```bash
node test-elevenlabs-api.js user
```

## 🔧 Troubleshooting

### Ошибка: "mic module is not installed"
```bash
npm install mic
brew install sox  # macOS
```

### Ошибка: "WebSocket connection failed"
- Проверьте API ключ
- Убедитесь, что есть интернет-соединение
- Проверьте, не истёк ли лимит символов

### Ошибка: "Microphone error"
- Дайте разрешение на микрофон
- Проверьте, что микрофон подключен
- Убедитесь, что SoX установлен

### Нет транскрипции при записи с микрофона
- Говорите громче
- Проверьте уровни микрофона в системе
- Попробуйте другой микрофон
- Измените язык в URL (строка 62): `language_code=ru` → `language_code=en`

## 📚 Дополнительная документация

- [ELEVENLABS_API_GUIDE.md](./ELEVENLABS_API_GUIDE.md) - Подробное руководство по API
- [docs/TRANSCRIPTION.md](./docs/TRANSCRIPTION.md) - Документация по транскрипции
- [docs/TRANSCRIPTION_SETUP.md](./docs/TRANSCRIPTION_SETUP.md) - Настройка транскрипции

## 🚀 Быстрый старт

```bash
# 1. Проверить аккаунт
node test-elevenlabs-api.js user

# 2. Установить зависимости для микрофона
npm install mic
brew install sox  # macOS

# 3. Запустить запись с микрофона
node test-scribe-realtime.js --mic

# 4. Говорите в микрофон, нажмите Q для завершения
```

## 💡 Советы

1. **Используйте Enter** для разделения длинных записей на фразы
2. **Проверяйте лимиты** перед большими задачами транскрипции
3. **Используйте правильный язык** в URL для лучшей точности
4. **Говорите чётко** для лучшего распознавания
5. **Следите за процентом использования** чтобы не превысить лимит

## 📈 Мониторинг использования

Добавьте alias для быстрой проверки лимитов:

```bash
# В ~/.bashrc или ~/.zshrc
alias el-limits='node /path/to/test-elevenlabs-api.js limits'
alias el-user='node /path/to/test-elevenlabs-api.js user'
```

Теперь можно быстро проверить:
```bash
el-limits
el-user
```

## 🎓 Обучение

Для изучения API рекомендуется:

1. Начать с `test-elevenlabs-api.js` - изучить структуру аккаунта
2. Попробовать `test-scribe-realtime.js` с файлом
3. Перейти к режиму микрофона
4. Интегрировать в основное приложение

---

**Автор:** Claude Code
**Дата:** 2025
**Версия:** 1.0
